package com.example.RainforestRetail.controllers;import com.example.RainforestRetail.models.Order;import com.example.RainforestRetail.repositories.OrderRepository;import com.example.RainforestRetail.services.OrderService;import com.example.RainforestRetail.services.ProductOrderService;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.http.HttpStatus;import org.springframework.http.ResponseEntity;import org.springframework.web.bind.annotation.*;import java.util.HashMap;import java.util.Map;@RestController@RequestMapping("/orders")public class OrderController {    @Autowired    OrderService orderService;    @Autowired    ProductOrderService productOrderService;//    create a new order    @PostMapping    public ResponseEntity <Order> addNewOrder(@RequestParam long id ){        Order order = orderService.createOrder(id);            return new  ResponseEntity<>(order, HttpStatus.CREATED);        }    @PatchMapping(value = "/{id}")    public ResponseEntity <Order> addProductOrdersToOrder (@RequestBody Map<Long, Integer> basket, @PathVariable long userId){        Order order = productOrderService.addProductsToOrder(userId, basket);        return new ResponseEntity<>(order, HttpStatus.CREATED);    }}