package com.example.RainforestRetail.controllers;import com.example.RainforestRetail.models.User;import com.example.RainforestRetail.services.UserService;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.http.HttpStatus;import org.springframework.http.ResponseEntity;import org.springframework.web.bind.annotation.*;@RestController@RequestMapping("/users")public class UserController {    @Autowired    UserService userService;    // Under Buy     @PostMapping    public ResponseEntity<User> addNewUser(@RequestBody User user) {        userService.createNewUser(user);        return new ResponseEntity<>(user, HttpStatus.CREATED);    }    @GetMapping(value = "/{id}")    public ResponseEntity<User> getUserById(@PathVariable long id) {        User foundUser = userService.getUserOrders(id);        return new ResponseEntity<>(foundUser, HttpStatus.OK);    }    @GetMapping //RequestParam for get mapping as opposed to using PathVariable    public ResponseEntity<User> getUserByUserEmail(@RequestParam String email) {        User userFoundByEmail = userService.findUserByEmail(email);        return new ResponseEntity<>(userFoundByEmail, HttpStatus.OK);    }}